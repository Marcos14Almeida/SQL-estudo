SELECT AVG(column_name)
FROM table_name
WHERE condition;

COUNT(), MAX(), MIN(), AVG(), SUM()

--------------------------------------
-- Mostra só um valor com a contagem, não mostra um
SELECT COUNT(ProductID)
FROM Products;

